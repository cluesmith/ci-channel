I’ll review this as a PR review, focusing on behavioral regressions and spec mismatches rather than re-summarizing the diff. I’m going to inspect the implementation and the tests from disk so line references are grounded in the actual files.I’ve read the core setup implementation and the setup tests. The overall shape matches the spec, so I’m checking less obvious runtime behavior now: process spawning edge cases, path construction, error classification, and docs claims against the actual code.The mechanical gates I could inspect from disk pass: `lib/setup.ts` is 294 lines, `tests/setup.test.ts` is 399 lines, and the setup test count is 18. I’m going to do one compile/test attempt where the sandbox permits it, but I expect full tests may be blocked because this environment is read-only and the tests create temp projects.The compile-only check passed (`npx tsc --noEmit`). The focused setup test run was blocked by sandbox permissions on `mkdtemp` under the OS temp directory, so I’m not treating that as a code failure; it failed before exercising the implementation.I found a substantive issue now: the installer still writes the same GitHub-default `.mcp.json` entry for every forge. I’m checking the runtime config path to confirm impact before I call it blocking, because if the channel starts without `--forge gitlab` or `--forge gitea`, the runtime will parse incoming webhooks as GitHub.**Findings**

1. [lib/setup.ts](/Users/mwk/Development/cluesmith/ci-channel/.builders/aspir-7-spec-7-feat-gitlab-gitea-insta/lib/setup.ts):9  
   The installer still registers the same `.mcp.json` entry for every forge: `npx -y ci-channel`, with no `--forge`, `--repos`, or `--gitea-url` args. That means a project installed with `ci-channel setup --forge gitlab ...` or `--forge gitea ...` will start the runtime with `loadConfig()` defaulting to `forge: "github"` ([server.ts](/Users/mwk/Development/cluesmith/ci-channel/.builders/aspir-7-spec-7-feat-gitlab-gitea-insta/server.ts):25 and [config.ts](/Users/mwk/Development/cluesmith/ci-channel/.builders/aspir-7-spec-7-feat-gitlab-gitea-insta/lib/config.ts):127). Incoming GitLab/Gitea webhooks will then be validated and parsed with the GitHub forge path, so the new installer flows create webhooks that the installed channel will reject or ignore. The tests currently lock in the wrong behavior via `MCP_CI_ONLY` assertions, for example [tests/setup.test.ts](/Users/mwk/Development/cluesmith/ci-channel/.builders/aspir-7-spec-7-feat-gitlab-gitea-insta/tests/setup.test.ts):243 and [tests/setup.test.ts](/Users/mwk/Development/cluesmith/ci-channel/.builders/aspir-7-spec-7-feat-gitlab-gitea-insta/tests/setup.test.ts):296. The setup path needs to write forge-specific MCP args at least for non-GitHub, and likely `--repos` for the configured repo plus `--gitea-url` for Gitea.

**Validation**

`npx tsc --noEmit` passed. I attempted `node --import tsx/esm --test tests/setup.test.ts`, but this read-only sandbox denied `mkdtemp` in the OS temp directory, so the focused test run failed before exercising the implementation. Mechanical gates inspected from disk: `lib/setup.ts` is 294 lines, `tests/setup.test.ts` is 399 lines, and setup tests count is 18.

---
VERDICT: REQUEST_CHANGES
SUMMARY: Non-GitHub setup registers a GitHub-default MCP server entry, so GitLab/Gitea installs won’t actually run as GitLab/Gitea at runtime.
CONFIDENCE: HIGH
---

KEY_ISSUES: Non-GitHub `.mcp.json` entries omit `--forge` and related runtime args.